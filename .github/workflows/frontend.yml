name: Frontend CI

on:
  push:
    branches: [main]
    paths:
      - "frontend/**"
      - "skyjo-core/**"
      - "skyjo-wasm/**"
      - "Cargo.toml"
      - "Cargo.lock"
  pull_request:
    branches: [main]
    paths:
      - "frontend/**"
      - "skyjo-core/**"
      - "skyjo-wasm/**"
      - "Cargo.toml"
      - "Cargo.lock"

concurrency:
  group: frontend-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-build:
    name: Lint & Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      # Rust + wasm-pack for WASM build
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-wasm-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-wasm-
      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh
      - name: Verify lockfile
        run: cargo check --locked --manifest-path skyjo-wasm/Cargo.toml
      - name: Build WASM
        run: cd skyjo-wasm && wasm-pack build --target web --out-dir ../frontend/pkg

      # Node + pnpm for frontend
      - uses: pnpm/action-setup@v6
        with:
          version: 11.0.0-rc.0
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml
      - name: Install dependencies
        run: cd frontend && pnpm install --frozen-lockfile
      - name: Type check
        run: cd frontend && pnpm lint
      - name: Test
        run: cd frontend && pnpm test
      - name: Build
        run: cd frontend && pnpm build
